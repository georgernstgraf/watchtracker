<!-- #watchgrid.hbs -->
<div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-3 mb-3">
    {{#each userWatches}}
    <div class="col">
        <div class="card h-100 border-secondary"
             style="cursor: pointer;"
             hx-get='{{../appPath}}/watch/{{id}}'
             hx-target='#mainContent'
             hx-swap='innerHTML'>
            <!-- Square Image Area -->
            <div class="position-relative ratio ratio-1x1">
                {{#if image}}
                <img src="{{../appPath}}/watch/image/{{id}}" 
                     class="card-img-top" 
                     alt="{{name}}"
                     style="object-fit: cover;">
                {{else}}
                <div class="d-flex align-items-center justify-content-center w-100 h-100"
                     style="background: linear-gradient(135deg, var(--bs-secondary-bg) 0%, var(--bs-border-color) 100%);">
                    <span class="display-4 text-white fw-bold">{{getInitials name}}</span>
                    <!-- Upload Indicator -->
                    <div class="position-absolute bottom-0 end-0 m-2">
                        <span class="badge bg-secondary">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-1.002v-1h-2v1H5.002v-1h-2v1zm13 12H1.002V6.5l2.5-3 3.5 4.5 2.5-3 4 5V13z"/>
                            </svg>
                        </span>
                    </div>
                </div>
                {{/if}}
            </div>
            <!-- Card Body -->
            <div class="card-body text-center py-2">
                <h5 class="card-title mb-1">{{name}}</h5>
                {{#if comment}}
                <p class="card-text small mb-1">{{comment}}</p>
                {{/if}}
                <div class="d-flex justify-content-around small mt-1">
                    {{#if precision}}<span title="Precision">{{precision}}</span>{{else}}<span>-</span>{{/if}}
                </div>
                <div class="d-flex justify-content-around small">
                    {{#if daysMeasured}}<span title="Days measured">{{daysMeasured}}d</span>{{else}}<span>-</span>{{/if}}
                    {{#if lastUsed}}<span title="Last used">{{formatDate lastUsed}}</span>{{else}}<span>-</span>{{/if}}
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</div>
<!-- /watchgrid.hbs -->
